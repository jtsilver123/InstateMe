{
  "name": "instateme-site",
  "version": "2.0.0",
  "private": true,
  "description": "instateme.com — main site (root) and acquisition microsite (/buy).",
  "scripts": {
    "build": "tsc && tsc -p buy",
    "typecheck": "tsc --noEmit && tsc -p buy --noEmit",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
